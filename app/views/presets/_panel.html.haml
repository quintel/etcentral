- value.flatten!
.panel
  .panel-label
    = value[0]
  .value
    - if value[2] == 'bln_euro'
      %span>= "€"
      = value[1].round(1)
    - if value[2] == 'factor' && value[0] != "Bio-footprint"
      - if value[0] == "Energy use" && value[1] > 0
        %span>= "+"
      = (value[1]*100).round(1)
      %span>= "%"
    - if value[2] == 'factor' && value[0] == "Bio-footprint"
      = value[1].round(1)
      = "x NL"